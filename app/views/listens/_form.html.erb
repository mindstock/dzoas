<%= form_for(@listen) do |f| %>
  <% if @listen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listen.errors.count, "error") %> prohibited this listen from being saved:</h2>

      <ul>
      <% @listen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plan_type %><br>
    <%= f.number_field :plan_type %>
  </div>
  <div class="field">
    <%= f.label :first_status %><br>
    <%= f.text_field :first_status %>
  </div>
  <div class="field">
    <%= f.label :last_status %><br>
    <%= f.text_field :last_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
